[project]
name = "codigo"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.2",
    "pydantic-ai[google]>=1.93.0",
    "python-dotenv>=1.2.2",
]
